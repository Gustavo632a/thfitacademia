import { ArrowUpRight, Clock3, MapPin } from 'lucide-react';
import { ADDRESS, MAPS_URL, hasLocation, contactUrl } from '../data/config';
import { schedule } from '../data/schedule';
export default function LocationSchedule() {
  return <section id="localizacao" className="location-section"><div className="container location-grid"><div className="location reveal"><div className="eyebrow">02 <span /> ONDE ESTAMOS</div><h2>VENHA CONHECER<br />A <em>TH FIT.</em></h2><p className="location-address"><MapPin size={18} />{hasLocation ? ADDRESS : 'Seu próximo passo começa aqui.'}</p>{hasLocation ? <iframe className="map" title="Localização da TH Fit Academia" src={`https://maps.google.com/maps?q=${encodeURIComponent(ADDRESS)}&output=embed`} loading="lazy" referrerPolicy="no-referrer" /> : <div className="map-placeholder"><div className="map-lines" /><span className="map-marker"><MapPin size={24} /></span><div><strong>Vem treinar com a gente.</strong><p>Fale com a equipe e consulte nossa localização.</p></div></div>}<a className="text-link" href={hasLocation ? MAPS_URL : contactUrl} target="_blank" rel="noopener noreferrer">{hasLocation ? 'Como chegar' : 'Consultar localização'}<ArrowUpRight size={18} /></a></div><div id="horarios" className="schedule reveal"><div className="eyebrow"><Clock3 size={16} /> HORÁRIO DE FUNCIONAMENTO</div><h2>TREINE NO<br />SEU <em>HORÁRIO.</em></h2><div className="schedule-list">{schedule.map(([day, start, end]) => <div className="schedule-row" key={day}><span>{day}</span><strong>{start}<span>—</span>{end}</strong></div>)}</div><p className="schedule-note"><span /> Mais tempo para cuidar de você.</p></div></div></section>;
}
